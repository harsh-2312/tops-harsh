// q6
#include <stdio.h>

int main() {
    

    // 6. Find area of Triangle Formula : A = 1/2 × b × h
    float base , height, area;

    printf("Enter the base of the triangle: ");
    scanf("%f", &base);

    printf("Enter the height of the triangal: ");
    scanf("%f", &height);

    
    printf("The area of the triangale is: %f\n", area = (base * height)/2);

    return 0;
}
